# Simple Voice Chat leaves speak/listen/groups with no plugin.yml default,
# which Bukkit treats as OP-only. Everyone should be able to use proximity voice.
voicechat.listen:
  default: true
voicechat.speak:
  default: true
voicechat.groups:
  default: true
