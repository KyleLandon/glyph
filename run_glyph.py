import sys
sys.path.insert(0, './glyph')

import main

# This will run main.main() from main.glyph (assuming it's transpiled or interpreted as Python for now)
# In a real bootstrapping scenario, you would have a Glyph interpreter here. 